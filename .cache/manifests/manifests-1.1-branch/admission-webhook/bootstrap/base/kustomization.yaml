---
apiVersion: kustomize.config.k8s.io/v1beta1
commonLabels:
  kustomize.component: admission-webhook-bootstrap
configMapGenerator:
- behavior: merge
  envs:
  - params.env
  name: config-map
configurations:
- params.yaml
generatorOptions:
  disableNameSuffixHash: true
images:
- name: docker.artifactory.ikarem.io/user/ravi/kubeflow/kubeflow-images-public/ingress-setup
  newName: docker.artifactory.ikarem.io/user/ravi/kubeflow/kubeflow-images-public/ingress-setup
  newTag: latest
kind: Kustomization
namePrefix: admission-webhook-bootstrap-
namespace: kubeflow
resources:
- cluster-role-binding.yaml
- cluster-role.yaml
- config-map.yaml
- service-account.yaml
- stateful-set.yaml
vars:
- fieldref:
    fieldpath: data.webhookNamePrefix
  name: webhookNamePrefix
  objref:
    apiVersion: v1
    kind: ConfigMap
    name: config-map
- fieldref:
    fieldpath: data.namespace
  name: namespace
  objref:
    apiVersion: v1
    kind: ConfigMap
    name: config-map
